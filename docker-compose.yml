services:
  app:
    build: .
    container_name: franchises-app
    ports:
      - "8080:8080"
    environment:
      MONGODB_URI: mongodb://host.docker.internal:27017/franchise_db
      PORT_SERVER: 8080
      ENV: prod